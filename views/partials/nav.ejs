<nav style="display:flex;gap:16px;margin-bottom:24px;">
  <a href="/">🏠 Home</a>
  <% if (locals.user) { %>
    <a href="/new-message">✏️ New Message</a>
    <% if (!user.is_member && !user.is_admin) { %><a href="/join">🔑 Join Club</a><% } %>
    <a href="/log-out">Logout (<%= user.email %>)</a>
  <% } else { %>
    <a href="/sign-up">Sign Up</a>
    <a href="/log-in">Log In</a>
  <% } %>
</nav>
